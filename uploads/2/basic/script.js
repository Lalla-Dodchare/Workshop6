// const คือค่าคงที่ (ใช้เป็นหลัก)
const name = "John";
const price = 150;

//let คือค่าที่เปลี่ยนได้ เช่น
let num = 0;
num = 1;

//ไม่ใช้ var แล้วเพราะเก่า
//โดยทั่วไปใช้ const ก่อน ถ้าจะเปลี่ยนค่อยเปลี่ยนเป็น let

/// ชนิดข้อมูล (Data Types)

// string
const text = "สวัสดี";

// number
const amount = 100.50;

const catinhome = {
    name: gogo,
    age: 15,
    color: black
}


